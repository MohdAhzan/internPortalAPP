package routes

import (
	"github.com/gin-gonic/gin"
)

func ReceptionistRoutes(engine *gin.RouterGroup,receptionistAuth gin.HandlerFunc){
    // engine.POST("/addpatient",h.re)
    // engine.POST("/login",h.UserLogin)
    
}
